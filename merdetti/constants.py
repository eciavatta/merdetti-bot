

LOGIN_CALLBACK, CANCEL_CALLBACK, ENTER_CALLBACK, EXIT_CALLBACK = (
    "login_callback",
    "cancel_callback",
    "enter_callback",
    "exit_callback",
)

(STATE, STARTED, CALLBACK_SESSION, LOGGED) = (
    "state",
    "started",
    "callback_session",
    "logged"
)

(STATE_WAITING_CREDENTIALS, STATE_MAIN) = (
    "waiting_credentials", "main"
)
